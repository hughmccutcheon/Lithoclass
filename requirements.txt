# Pinned environment for lithoclass (Python 3.14.0, Windows)
# Rebuild: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt -e .
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
matplotlib==3.11.0
pyarrow==24.0.0
pytest==9.1.1
ruff==0.15.20

# Phase 6 stretch only (NVCL hyperspectral notebook); not needed for the
# core geochemistry pipeline
nvcl_kit==1.3.0
jupyter==1.1.1
nbconvert==7.17.1
