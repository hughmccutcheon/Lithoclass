# Pinned environment for lithoclass (Python 3.14.0, Windows)
# Rebuild: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt -e .
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
matplotlib==3.11.0
pyarrow==24.0.0
pytest==9.1.1
ruff==0.15.20
